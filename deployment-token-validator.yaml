apiVersion: apps/v1
kind: Deployment
metadata:
  name: sdone_token-validator
spec:
  replicas: 3
  selector:
    matchLabels:
      app: sdone_token-validator
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
  minReadySeconds: 5
  template:
    metadata:
      labels:
        app: sdone_token-validator
    spec:
      containers:
        - name: app
          image: ghcr.io/ahmadirfaan/token-validator:1.1.0
          ports:
            - containerPort: 40001
          env:
            - name: PORT_GRPC
              value: '40001'
          envFrom:
            - configMapRef:
                name: sdone-cm
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 250m
              memory: 512Mi